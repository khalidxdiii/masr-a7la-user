import 'package:flutter/material.dart';

class ReportStateScreen extends StatelessWidget {
  const ReportStateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'User Report State Screen',
        style: TextStyle(
          fontSize: 25.0,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}
